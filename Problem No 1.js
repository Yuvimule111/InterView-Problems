// Given an array if a name of country , you are supposed to sort it in lexicographical order using the selection sort 

// Input : ["India", "Australia", "China", "Russia", "Brazil", "Japan"]

// OutPut : ["Austr"]
/*
function selectionSort (inputArray) 
    var i, j, minimumindex;
    for(i = 0; i < inputArray.length - 1; i++) {
        minimumindex = i;
        for(j = i + 1; j < inputArray.length; j++) {
            if(inputArray[j].localeCompare(inputArray[minimumindex]) == -1)

        

        {
            minimumindex = j;
        }
    }
}


*/

let no = 10;
console.log(++no, no++);
